# netlify.toml

# 将名为 "random-redirect" 的 Edge Function 映射到 /random 路径
[[edge_functions]]
  function = "random-redirect" # 对应你的文件 netlify/edge-functions/random-redirect.js
  path = "/random"             # 当用户访问 https://你的域名/random 时触发此函数
